# Alibaba Cloud Credentials for C++ - 用户使用指南

## 1. 项目简介

**Alibaba Cloud Credentials for C++** 是阿里云官方提供的 C++ 凭证管理工具库，旨在帮助 C++ 开发者安全、便捷地管理和获取阿里云访问凭证。

### 核心能力

- **多种凭证类型支持**：AccessKey、STS Token、ECS RAM 角色、RAM 角色扮演、RSA 密钥对、Bearer Token、OIDC、CloudSSO、OAuth 等
- **默认凭证提供者链**：自动按优先级从多个来源查找凭证（环境变量 → OIDC → 配置文件 → ECS 实例角色 → URI）
- **自动刷新机制**：对于临时凭证（如 STS Token、ECS RAM 角色），支持自动刷新防止过期
- **跨平台支持**：Windows、Linux、macOS 全平台兼容
- **安全特性**：支持 IMDSv2（ECS 元数据服务安全强化模式）、配置文件权限检查

### 典型应用场景

1. **云上应用**：在阿里云 ECS 实例上运行的应用，使用实例 RAM 角色自动获取临时凭证
2. **本地开发**：开发者在本地通过环境变量或配置文件设置 AccessKey 进行测试
3. **CI/CD 流水线**：在持续集成环境中通过环境变量注入临时 STS 凭证
4. **多账号管理**：通过配置文件管理多套凭证，支持不同环境切换
5. **安全加固**：使用 RAM 角色扮演、OIDC 等方式避免长期 AccessKey 泄露风险

---

## 2. 使用前提与环境要求

### 2.1 操作系统要求

- **Linux**：主流发行版（Ubuntu 16.04+、CentOS 7+、Debian 9+）
- **macOS**：10.12（Sierra）及以上版本
- **Windows**：Windows 7 及以上（推荐 Windows 10/11）

### 2.2 编译器要求

| 平台    | 编译器要求                          |
|---------|-------------------------------------|
| Windows | Visual Studio 2015 或更高版本       |
| Linux   | GCC 4.9 或更高版本                  |
| macOS   | Clang（通过 Xcode Command Line Tools）|

**重要**：编译器必须支持 C++11 或更高标准。

### 2.3 构建工具

- **CMake**：3.5 或更高版本（推荐 3.10+）
  - 安装方法：
    - Ubuntu/Debian: `sudo apt-get install cmake`
    - CentOS/RHEL: `sudo yum install cmake`
    - macOS: `brew install cmake`
    - Windows: 从 [cmake.org](https://cmake.org/download/) 下载安装包

### 2.4 系统依赖库

#### 必需依赖

**OpenSSL**（用于加密和网络通信）

- **Windows**:
  ```powershell
  # 使用 vcpkg（推荐）
  vcpkg install openssl:x64-windows
  
  # 或使用 Chocolatey
  choco install openssl
  ```

- **Linux**:
  ```bash
  # Ubuntu/Debian
  sudo apt-get install libssl-dev
  
  # CentOS/RHEL
  sudo yum install openssl-devel
  
  # Fedora
  sudo dnf install openssl-devel
  ```

- **macOS**:
  ```bash
  brew install openssl
  ```

#### 可选依赖

**nlohmann_json**（用于支持 JSON 格式的配置文件）

- **作用**：如果安装此库，CLIProfileProvider 将支持读取 `~/.alibabacloud/credentials.json` 格式的配置文件
- **不安装的影响**：仅支持 INI 格式的配置文件 `~/.alibabacloud/credentials.ini`

- **安装方法**:
  ```bash
  # Windows (vcpkg)
  vcpkg install nlohmann-json:x64-windows
  
  # Ubuntu/Debian
  sudo apt-get install nlohmann-json3-dev
  
  # macOS
  brew install nlohmann-json
  ```

### 2.5 网络与权限要求

#### 网络访问

- **STS 服务**：需要访问 `sts.aliyuncs.com`（或其他区域端点）
- **ECS 元数据服务**：在 ECS 实例上需要访问 `100.100.100.200`
- **OIDC/OAuth 服务**：需要访问相应的认证服务器

#### 权限要求

- **配置文件读取**：需要读取 `~/.alibabacloud/` 目录下的配置文件
- **环境变量访问**：需要读取 `ALIBABA_CLOUD_*` 等环境变量
- **临时文件**：部分场景可能需要在用户目录创建临时文件

**注意**：不需要 root/管理员权限（除非安装到系统目录）。

### 2.6 阿里云账号要求

- 已注册阿里云账号
- 已创建 AccessKey（访问密钥）或配置了相应的 RAM 角色
- 获取凭证地址：[阿里云控制台 - AccessKey 管理](https://usercenter.console.aliyun.com/#/manage/ak)

---

## 3. 快速开始指南

### 3.1 安装与构建

#### 方法一：Linux 一键安装脚本

```bash
# 克隆代码
git clone https://github.com/aliyun/credentials-cpp.git
cd credentials-cpp

# 执行安装脚本（需要 sudo 权限）
sh scripts/install.sh
```

**说明**：此脚本会编译代码并安装到 `/usr/local`。

#### 方法二：手动构建（跨平台）

**Linux / macOS**:

```bash
# 克隆代码
git clone https://github.com/aliyun/credentials-cpp.git
cd credentials-cpp

# 创建构建目录
mkdir -p cmake_build
cd cmake_build

# 配置项目
cmake -S .. -B . -DCMAKE_BUILD_TYPE=Release -DENABLE_UNIT_TESTS=OFF

# 编译
cmake --build . --config Release

# 安装（可选，需要 sudo）
sudo cmake --install .
```

**Windows (PowerShell)**:

```powershell
# 克隆代码
git clone https://github.com/aliyun/credentials-cpp.git
cd credentials-cpp

# 创建构建目录
mkdir cmake_build
cd cmake_build

# 配置项目（使用 Visual Studio）
cmake -S .. -B . -DCMAKE_BUILD_TYPE=Release -DENABLE_UNIT_TESTS=OFF

# 编译
cmake --build . --config Release

# 安装（可选）
cmake --install .
```

#### 方法三：Windows Visual Studio GUI

1. 使用 CMake GUI：
   - **Browse Source**: 选择源代码根目录
   - **Browse Build**: 选择构建目录（如 `cmake_build`）
   - 点击 **Configure**，选择 Visual Studio 版本
   - 点击 **Generate**，生成 `.sln` 解决方案文件

2. 使用 Visual Studio 打开 `cmake_build/AlibabaCloud_credentials.sln`

3. 选择 **Release** 配置，勾选 **INSTALL** 项目，点击 **生成解决方案**

### 3.2 配置说明

#### 配置方式 1: 环境变量（推荐用于开发/测试）

**Linux / macOS**:

```bash
export ALIBABA_CLOUD_ACCESS_KEY_ID="<your-access-key-id>"
export ALIBABA_CLOUD_ACCESS_KEY_SECRET="<your-access-key-secret>"
```

**Windows (PowerShell)**:

```powershell
$env:ALIBABA_CLOUD_ACCESS_KEY_ID="<your-access-key-id>"
$env:ALIBABA_CLOUD_ACCESS_KEY_SECRET="<your-access-key-secret>"
```

**Windows (CMD)**:

```cmd
set ALIBABA_CLOUD_ACCESS_KEY_ID=<your-access-key-id>
set ALIBABA_CLOUD_ACCESS_KEY_SECRET=<your-access-key-secret>
```

#### 配置方式 2: 配置文件（推荐用于生产环境）

**INI 格式** - 创建 `~/.alibabacloud/credentials.ini`:

```ini
[default]
enable = true
type = access_key
access_key_id = <your-access-key-id>
access_key_secret = <your-access-key-secret>
```

**JSON 格式** - 创建 `~/.alibabacloud/credentials.json`（需要安装 nlohmann_json）:

```json
{
  "mode": "AK",
  "accessKeyId": "<your-access-key-id>",
  "accessKeySecret": "<your-access-key-secret>"
}
```

**注意**：
- Windows 用户主目录通常为 `C:\Users\<用户名>\`
- Linux/macOS 用户主目录为 `/home/<用户名>/` 或 `/Users/<用户名>/`
- 配置文件权限建议设置为 `600`（仅所有者可读写）

### 3.3 快速示例代码

#### 示例 1: 使用默认凭证提供者链（推荐）

```cpp
#include <alibabacloud/credential/Credential.hpp>
#include <iostream>

using namespace AlibabaCloud::Credential;

int main() {
    try {
        // 使用默认凭证提供者链，自动查找凭证
        Client client;
        
        // 获取凭证
        auto credential = client.getCredential();
        
        std::cout << "AccessKeyId: " << credential.accessKeyId() << std::endl;
        std::cout << "Type: " << credential.type() << std::endl;
        
        return 0;
    } catch (const std::exception& e) {
        std::cerr << "错误: " << e.what() << std::endl;
        return 1;
    }
}
```

**编译命令**:

```bash
g++ -std=c++11 example.cpp -lalibabacloud_credential -lssl -lcrypto -o example
./example
```

**预期输出**（如果已配置环境变量）:

```
AccessKeyId: LTAI***********
Type: access_key
```

#### 示例 2: 显式指定 AccessKey

```cpp
#include <alibabacloud/credential/Credential.hpp>
#include <iostream>

using namespace AlibabaCloud::Credential;

int main() {
    try {
        // 创建配置
        Models::Config config;
        config.setType("access_key")
              .setAccessKeyId("LTAI***********")
              .setAccessKeySecret("*********************");
        
        // 创建客户端
        Client client(config);
        
        // 获取凭证
        auto credential = client.getCredential();
        
        std::cout << "AccessKeyId: " << credential.accessKeyId() << std::endl;
        std::cout << "Type: " << credential.type() << std::endl;
        
        return 0;
    } catch (const std::exception& e) {
        std::cerr << "错误: " << e.what() << std::endl;
        return 1;
    }
}
```

#### 示例 3: 在 ECS 实例上使用 RAM 角色

```cpp
#include <alibabacloud/credential/Credential.hpp>
#include <iostream>

using namespace AlibabaCloud::Credential;

int main() {
    try {
        // 配置 ECS RAM 角色凭证
        Models::Config config;
        config.setType("ecs_ram_role")
              .setRoleName("MyEcsRamRole");
        
        Client client(config);
        
        // 获取凭证（自动刷新）
        auto credential = client.getCredential();
        
        std::cout << "AccessKeyId: " << credential.accessKeyId() << std::endl;
        std::cout << "SecurityToken: " << credential.securityToken() << std::endl;
        
        return 0;
    } catch (const std::exception& e) {
        std::cerr << "错误: " << e.what() << std::endl;
        return 1;
    }
}
```

### 3.4 验证安装

运行单元测试以验证库是否正确安装：

```bash
# 克隆代码并构建测试
git clone https://github.com/aliyun/credentials-cpp.git
cd credentials-cpp
sh scripts/run_ut.sh
```

**预期输出**：所有测试用例通过（`[PASSED]`）。

---

## 4. 常见问题解答（FAQ）

### 4.1 编译与构建问题

#### Q1: CMake 提示 "OpenSSL not found"

**现象**：
```
CMake Error: OpenSSL not found. Please install OpenSSL
```

**根本原因**：
系统未安装 OpenSSL 开发库，或 CMake 无法找到 OpenSSL 的安装路径。

**解决方案**：

1. **确认 OpenSSL 已安装**：
   ```bash
   # Linux
   dpkg -l | grep libssl-dev  # Ubuntu/Debian
   rpm -qa | grep openssl-devel  # CentOS/RHEL
   
   # macOS
   brew list openssl
   
   # Windows (vcpkg)
   vcpkg list | findstr openssl
   ```

2. **安装 OpenSSL**（参见 2.4 节）

3. **手动指定 OpenSSL 路径**（如果安装在非标准位置）：
   ```bash
   # Linux/macOS
   cmake -S . -B build -DOPENSSL_ROOT_DIR=/usr/local/opt/openssl
   
   # Windows (vcpkg)
   cmake -S . -B build -DCMAKE_TOOLCHAIN_FILE=C:/vcpkg/scripts/buildsystems/vcpkg.cmake
   ```

---

#### Q2: Windows 编译时出现 "LNK2038: 检测到运行时库不匹配"

**现象**：
```
error LNK2038: 检测到"RuntimeLibrary"的不匹配项: 值"MTd_StaticDebug"不匹配值"MDd_DynamicDebug"
```

**根本原因**：
项目主库与依赖库（如 GoogleTest、OpenSSL）使用了不同的 C++ 运行时库（静态 `/MT` vs 动态 `/MD`）。

**解决方案**：

1. **使用项目推荐的配置**（已在 CMakeLists.txt 中强制使用 `/MD`）：
   ```bash
   # 清理旧的构建缓存
   rm -rf cmake_build
   mkdir cmake_build && cd cmake_build
   cmake -S .. -B . -DCMAKE_BUILD_TYPE=Release
   ```

2. **确保 vcpkg 依赖使用动态库**：
   ```bash
   vcpkg install openssl:x64-windows  # 动态库
   # 避免使用 openssl:x64-windows-static
   ```

3. **检查 CMakeCache.txt** 中的 `CMAKE_MSVC_RUNTIME_LIBRARY` 应为 `MultiThreadedDLL`

---

#### Q3: Linux 编译时提示 "undefined reference to `pthread_create`"

**现象**：
```
undefined reference to `pthread_create'
undefined reference to `dlopen'
```

**根本原因**：
未链接 POSIX 线程库（pthread）和动态链接库（dl）。

**解决方案**：

此问题已在 CMakeLists.txt 中解决（第 200-201 行自动链接 `pthread` 和 `dl`）。如果仍出现问题：

1. **手动添加链接选项**：
   ```bash
   g++ -std=c++11 your_code.cpp -lalibabacloud_credential -lssl -lcrypto -lpthread -ldl -o your_app
   ```

2. **检查 CMake 版本**（需要 3.5+）：
   ```bash
   cmake --version
   ```

---

#### Q4: macOS 编译时警告 "OpenSSL is deprecated"

**现象**：
```
warning: 'SSL_library_init' is deprecated [-Wdeprecated-declarations]
```

**根本原因**：
macOS 系统自带的 OpenSSL 已被标记为过时，Apple 建议使用 CommonCrypto。

**解决方案**：

1. **忽略警告**（不影响功能）：CMakeLists.txt 已添加 `-Wno-deprecated-declarations` 标志

2. **使用 Homebrew 安装的 OpenSSL**：
   ```bash
   brew install openssl
   cmake -S . -B build -DOPENSSL_ROOT_DIR=/usr/local/opt/openssl
   ```

---

#### Q5: 构建时提示 "nlohmann_json not found"

**现象**：
```
CMake Warning: nlohmann_json not found, CLIProfileProvider will only support INI format
```

**根本原因**：
这是一个警告（不是错误），表示未安装可选依赖 nlohmann_json，JSON 格式的配置文件将不被支持。

**解决方案**：

1. **如果不需要 JSON 格式配置文件**：忽略此警告，继续使用 INI 格式

2. **如果需要支持 JSON 格式**：
   ```bash
   # 安装 nlohmann_json（参见 2.4 节）
   # 然后重新运行 CMake
   rm CMakeCache.txt
   cmake -S . -B build
   ```

---

### 4.2 凭证配置问题

#### Q6: 使用默认凭证提供者链时，无法获取到凭证

**现象**：
```cpp
Client client;  // 默认构造
auto cred = client.getCredential();  // 抛出异常或返回空凭证
```

**根本原因**：
默认凭证提供者链按以下顺序查找凭证，如果所有来源都未配置，将无法获取凭证：
1. 环境变量（`ALIBABA_CLOUD_ACCESS_KEY_ID` 等）
2. OIDC 配置（环境变量）
3. 配置文件（`~/.alibabacloud/credentials.ini` 或 `.json`）
4. ECS 实例 RAM 角色
5. Credentials URI

**解决方案**：

1. **检查环境变量是否设置**：
   ```bash
   echo $ALIBABA_CLOUD_ACCESS_KEY_ID  # Linux/macOS
   echo %ALIBABA_CLOUD_ACCESS_KEY_ID%  # Windows CMD
   $env:ALIBABA_CLOUD_ACCESS_KEY_ID  # Windows PowerShell
   ```

2. **检查配置文件是否存在且格式正确**：
   ```bash
   # Linux/macOS
   cat ~/.alibabacloud/credentials.ini
   
   # Windows
   type %USERPROFILE%\.alibabacloud\credentials.ini
   ```

3. **配置文件必须包含 `enable = true`**：
   ```ini
   [default]
   enable = true  # 必需！
   type = access_key
   access_key_id = xxx
   access_key_secret = yyy
   ```

4. **检查配置文件权限**（Linux/macOS）：
   ```bash
   chmod 600 ~/.alibabacloud/credentials.ini
   ```

5. **如果在 ECS 上运行**，确认实例已绑定 RAM 角色：
   - 登录 ECS 控制台
   - 查看实例详情 → RAM 角色
   - 如未绑定，参考[文档](https://help.aliyun.com/document_detail/54579.html)绑定角色

---

#### Q7: 配置文件中的凭证未生效，仍在使用环境变量

**现象**：
明明配置文件中写了 AccessKey，但程序使用的是环境变量中的 AccessKey。

**根本原因**：
默认凭证提供者链的优先级是：**环境变量 > OIDC > 配置文件 > ECS 角色 > URI**。环境变量优先级最高。

**解决方案**：

1. **清除环境变量**：
   ```bash
   # Linux/macOS
   unset ALIBABA_CLOUD_ACCESS_KEY_ID
   unset ALIBABA_CLOUD_ACCESS_KEY_SECRET
   
   # Windows PowerShell
   Remove-Item Env:ALIBABA_CLOUD_ACCESS_KEY_ID
   Remove-Item Env:ALIBABA_CLOUD_ACCESS_KEY_SECRET
   ```

2. **或者显式指定凭证类型**，不使用默认链：
   ```cpp
   Models::Config config;
   config.setType("access_key")
         .setAccessKeyId("...")
         .setAccessKeySecret("...");
   Client client(config);
   ```

---

#### Q8: 配置文件路径错误或无法找到

**现象**：
```
Can't open credential profile: /home/user/.alibabacloud/credentials.ini
```

**根本原因**：
- 配置文件路径不正确
- 配置文件不存在
- 权限不足无法读取

**解决方案**：

1. **确认用户主目录**：
   ```bash
   # Linux/macOS
   echo $HOME
   
   # Windows
   echo %USERPROFILE%
   ```

2. **手动创建目录和文件**：
   ```bash
   # Linux/macOS
   mkdir -p ~/.alibabacloud
   touch ~/.alibabacloud/credentials.ini
   chmod 600 ~/.alibabacloud/credentials.ini
   
   # Windows PowerShell
   New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\.alibabacloud"
   New-Item -ItemType File -Path "$env:USERPROFILE\.alibabacloud\credentials.ini"
   ```

3. **通过环境变量指定自定义路径**：
   ```bash
   export ALIBABA_CLOUD_CREDENTIALS_FILE=/path/to/custom/credentials.ini
   ```

4. **检查文件读取权限**：
   ```bash
   ls -l ~/.alibabacloud/credentials.ini  # 应显示 -rw-------
   ```

---

#### Q9: JSON 格式配置文件不生效

**现象**：
创建了 `~/.alibabacloud/credentials.json` 文件，但程序提示找不到凭证。

**根本原因**：
1. 未安装 nlohmann_json 库，程序不支持 JSON 格式
2. JSON 格式错误（语法错误、字段名不匹配）

**解决方案**：

1. **确认是否支持 JSON**：
   - 重新编译时检查 CMake 输出：
     ```
     Found nlohmann_json, enabling JSON support for CLIProfileProvider
     ```
   - 如未显示此消息，表示不支持 JSON，需安装 nlohmann_json（参见 2.4 节）

2. **检查 JSON 语法**：
   ```bash
   # 使用 jq 或在线工具验证 JSON 格式
   cat ~/.alibabacloud/credentials.json | jq .
   ```

3. **确认 JSON 字段名正确**（区分大小写）：
   ```json
   {
     "mode": "AK",           # 正确
     "accessKeyId": "...",   # 正确（驼峰命名）
     "accessKeySecret": "..." # 正确
   }
   ```
   
   **错误示例**：
   ```json
   {
     "type": "access_key",    # 错误！应为 "mode"
     "access_key_id": "..."   # 错误！应为 "accessKeyId"
   }
   ```

---

### 4.3 ECS 实例 RAM 角色问题

#### Q10: 在 ECS 实例上无法获取 RAM 角色凭证

**现象**：
```
Failed to get RAM session credentials from ECS metadata service. HttpCode=404
```

**根本原因**：
1. ECS 实例未绑定 RAM 角色
2. 角色名配置错误
3. 元数据服务未启用或被禁用
4. 网络问题无法访问 `100.100.100.200`

**解决方案**：

1. **确认实例已绑定 RAM 角色**：
   - 登录 ECS 控制台 → 实例详情 → 查看 "实例 RAM 角色"
   - 如未绑定，参考[文档](https://help.aliyun.com/document_detail/54579.html)创建并绑定

2. **检查角色名是否正确**：
   ```cpp
   Models::Config config;
   config.setType("ecs_ram_role")
         .setRoleName("正确的角色名称");  // 必须与 ECS 绑定的角色名一致
   ```
   
   或通过环境变量设置：
   ```bash
   export ALIBABA_CLOUD_ECS_METADATA="正确的角色名称"
   ```

3. **测试元数据服务连通性**（在 ECS 实例上）：
   ```bash
   curl http://100.100.100.200/latest/meta-data/ram/security-credentials/
   # 应返回角色名称
   
   curl http://100.100.100.200/latest/meta-data/ram/security-credentials/<角色名>
   # 应返回 JSON 格式的临时凭证
   ```

4. **检查是否被环境变量禁用**：
   ```bash
   echo $ALIBABA_CLOUD_ECS_METADATA_DISABLED  # 应为空或 "false"
   
   # 如果为 "true"，清除或修改：
   unset ALIBABA_CLOUD_ECS_METADATA_DISABLED
   ```

---

#### Q11: IMDSv2 模式下获取凭证失败

**现象**：
```
Failed to get token from ECS Metadata Service. HttpCode=401
Failed to get RAM session credentials from ECS metadata service. HttpCode=401
```

**根本原因**：
ECS 实例元数据服务配置为仅允许 IMDSv2（安全强化模式），但客户端未正确获取或传递 Token。

**解决方案**：

1. **检查 IMDSv2 是否强制启用**：
   - 登录 ECS 控制台 → 实例详情 → 元数据选项
   - 如 "Metadata HTTP Tokens" 为 "required"，表示强制 IMDSv2

2. **确认代码未禁用 IMDSv2**：
   ```cpp
   Models::Config config;
   config.setType("ecs_ram_role")
         .setRoleName("MyRole")
         .setDisableIMDSv1(false);  // false 表示允许回退到 IMDSv1
   ```

3. **如果需要强制使用 IMDSv2**：
   ```cpp
   config.setDisableIMDSv1(true);  // 禁止回退到 IMDSv1
   ```
   
   或通过环境变量：
   ```bash
   export ALIBABA_CLOUD_IMDSV1_DISABLED=true
   ```

4. **检查网络策略**：
   - 确保安全组规则允许访问元数据服务（`100.100.100.200:80`）
   - 某些安全加固工具可能拦截元数据服务访问

---

#### Q12: ECS 角色凭证频繁过期

**现象**：
程序运行一段时间后，凭证失效，API 调用返回 "InvalidAccessKeyId.NotFound"。

**根本原因**：
ECS RAM 角色凭证是临时凭证，有效期通常为 1 小时。如果程序缓存凭证未及时刷新，会导致过期。

**解决方案**：

1. **使用 `getCredential()` 方法，自动刷新**：
   ```cpp
   Client client(config);
   
   // 每次调用前获取最新凭证
   while (true) {
       auto credential = client.getCredential();  // 自动刷新
       // 使用 credential 调用 API
       
       std::this_thread::sleep_for(std::chrono::minutes(10));
   }
   ```

2. **避免缓存 `accessKeyId()` 等字段**：
   ```cpp
   // 错误做法：缓存可能导致过期
   std::string cachedAk = client.getAccessKeyId();
   
   // 正确做法：每次获取最新凭证
   auto credential = client.getCredential();
   std::string currentAk = credential.accessKeyId();
   ```

3. **检查凭证过期时间**（调试用）：
   ```cpp
   // 注意：SDK 内部自动处理刷新，无需手动检查过期时间
   // 仅在调试时查看日志
   ```

---

### 4.4 网络与超时问题

#### Q13: 网络超时导致获取凭证失败

**现象**：
```
timeout: connect timeout (5000ms)
timeout: read timeout (10000ms)
```

**根本原因**：
1. 网络环境不佳（如弱网环境、跨国访问）
2. 防火墙/代理阻止访问 STS 服务或元数据服务
3. 默认超时时间（连接 10s，读取 5s）过短

**解决方案**：

1. **增加超时时间**：
   ```cpp
   Models::Config config;
   config.setType("ecs_ram_role")
         .setRoleName("MyRole")
         .setConnectTimeout(30000)  // 连接超时 30 秒
         .setTimeout(60000);        // 读取超时 60 秒
   ```

2. **检查网络连通性**：
   ```bash
   # 测试 STS 服务
   ping sts.aliyuncs.com
   curl https://sts.aliyuncs.com
   
   # 测试元数据服务（在 ECS 上）
   ping 100.100.100.200
   curl http://100.100.100.200/latest/meta-data/
   ```

3. **配置代理**（如果在企业网络内）：
   ```cpp
   config.setProxy("http://proxy.company.com:8080");
   ```
   
   或通过环境变量：
   ```bash
   export HTTP_PROXY=http://proxy.company.com:8080
   export HTTPS_PROXY=http://proxy.company.com:8080
   ```

4. **检查防火墙规则**：
   - 确保允许访问 `sts.aliyuncs.com:443`
   - 在 ECS 上允许访问 `100.100.100.200:80`

---

#### Q14: STS 服务返回 5xx 错误

**现象**：
```
Failed to refresh credentials. HttpCode=500
Failed to refresh credentials. HttpCode=503
```

**根本原因**：
STS 服务临时故障或过载。

**解决方案**：

1. **实现重试机制**：
   ```cpp
   int retryCount = 0;
   const int maxRetries = 3;
   
   while (retryCount < maxRetries) {
       try {
           auto credential = client.getCredential();
           break;  // 成功，退出循环
       } catch (const Darabonba::Exception& e) {
           if (std::string(e.what()).find("HttpCode=5") != std::string::npos) {
               retryCount++;
               std::this_thread::sleep_for(std::chrono::seconds(2 * retryCount));
           } else {
               throw;  // 非 5xx 错误，直接抛出
           }
       }
   }
   ```

2. **使用备用区域 STS endpoint**（如果主区域故障）：
   ```cpp
   config.setStsRegionId("cn-shanghai");  // 切换到其他区域
   ```

3. **查看阿里云服务状态**：[status.aliyun.com](https://status.aliyun.com/)

---

### 4.5 凭证安全问题

#### Q15: AccessKey 泄露到代码仓库

**现象**：
不小心将 AccessKey 硬编码到源代码中，提交到 Git 仓库。

**根本原因**：
缺乏安全意识，将敏感信息直接写入代码。

**解决方案**：

1. **立即撤销泄露的 AccessKey**：
   - 登录阿里云控制台 → AccessKey 管理
   - 禁用或删除泄露的 AccessKey
   - 创建新的 AccessKey

2. **从 Git 历史中清除敏感信息**：
   ```bash
   # 使用 git-filter-branch 或 BFG Repo-Cleaner
   git filter-branch --force --index-filter \
     "git rm --cached --ignore-unmatch path/to/file.cpp" \
     --prune-empty --tag-name-filter cat -- --all
   
   git push origin --force --all
   ```

3. **使用推荐的安全方式**：
   - **环境变量**（开发/测试）
   - **配置文件**（生产环境，确保权限 600）
   - **ECS RAM 角色**（云上应用）
   - **OIDC/CloudSSO**（企业身份联邦）

4. **添加到 `.gitignore`**：
   ```gitignore
   # 敏感配置文件
   *.ini
   credentials.json
   .alibabacloud/
   ```

5. **使用 pre-commit 钩子检测敏感信息**：
   ```bash
   # 安装 git-secrets 工具
   brew install git-secrets  # macOS
   
   git secrets --install
   git secrets --register-aws  # 检测 AccessKey 格式
   ```

---

#### Q16: 配置文件权限过于宽松

**现象**：
Linux/macOS 系统上配置文件权限为 `644`（所有用户可读），存在安全风险。

**根本原因**：
创建配置文件时未设置合适的权限。

**解决方案**：

1. **修改文件权限**：
   ```bash
   chmod 600 ~/.alibabacloud/credentials.ini
   chmod 600 ~/.alibabacloud/credentials.json
   ```

2. **验证权限**：
   ```bash
   ls -l ~/.alibabacloud/
   # 应显示 -rw------- (600)
   ```

3. **在代码中检查权限**（可选，SDK 未内置此功能）：
   ```cpp
   #include <sys/stat.h>
   
   void checkFilePermissions(const std::string& path) {
       struct stat fileStat;
       if (stat(path.c_str(), &fileStat) == 0) {
           mode_t perms = fileStat.st_mode & 0777;
           if (perms != 0600) {
               std::cerr << "警告：配置文件权限过于宽松！" << std::endl;
           }
       }
   }
   ```

---

### 4.6 多环境与多账号管理

#### Q17: 如何在同一台机器上管理多套凭证（多环境）

**现象**：
开发、测试、生产环境需要使用不同的 AccessKey。

**解决方案**：

1. **方法一：使用配置文件的多个 section**（推荐）：

   创建 `~/.alibabacloud/credentials.ini`：
   ```ini
   [development]
   enable = true
   type = access_key
   access_key_id = DEV_AK_ID
   access_key_secret = DEV_AK_SECRET
   
   [production]
   enable = true
   type = access_key
   access_key_id = PROD_AK_ID
   access_key_secret = PROD_AK_SECRET
   ```
   
   通过环境变量切换：
   ```bash
   export ALIBABA_CLOUD_PROFILE=development  # 使用开发环境
   # 或
   export ALIBABA_CLOUD_PROFILE=production   # 使用生产环境
   ```

2. **方法二：使用不同的配置文件**：
   ```bash
   # 创建多个配置文件
   ~/.alibabacloud/credentials-dev.ini
   ~/.alibabacloud/credentials-prod.ini
   
   # 通过环境变量指定
   export ALIBABA_CLOUD_CREDENTIALS_FILE=~/.alibabacloud/credentials-dev.ini
   ```

3. **方法三：在代码中显式指定**：
   ```cpp
   Models::Config devConfig;
   devConfig.setAccessKeyId("DEV_AK").setAccessKeySecret("DEV_SECRET");
   
   Models::Config prodConfig;
   prodConfig.setAccessKeyId("PROD_AK").setAccessKeySecret("PROD_SECRET");
   
   // 根据运行环境选择
   Client client(isDevelopment ? devConfig : prodConfig);
   ```

---

#### Q18: 如何在代码中切换不同的凭证类型

**现象**：
需要在同一程序中使用不同类型的凭证（如测试时用 AccessKey，生产用 ECS 角色）。

**解决方案**：

```cpp
#include <alibabacloud/credential/Credential.hpp>
#include <cstdlib>

using namespace AlibabaCloud::Credential;

Client createClient() {
    const char* env = std::getenv("APP_ENV");
    std::string environment = env ? env : "production";
    
    if (environment == "development") {
        // 开发环境：使用 AccessKey
        Models::Config config;
        config.setType("access_key")
              .setAccessKeyId("DEV_AK")
              .setAccessKeySecret("DEV_SECRET");
        return Client(config);
    } else if (environment == "testing") {
        // 测试环境：使用 STS
        Models::Config config;
        config.setType("sts")
              .setAccessKeyId("TEST_AK")
              .setAccessKeySecret("TEST_SECRET")
              .setSecurityToken("TEST_TOKEN");
        return Client(config);
    } else {
        // 生产环境：使用 ECS RAM 角色
        Models::Config config;
        config.setType("ecs_ram_role");
        return Client(config);
    }
}

int main() {
    Client client = createClient();
    // 使用 client
}
```

运行时指定环境：
```bash
APP_ENV=development ./myapp  # 开发环境
APP_ENV=testing ./myapp      # 测试环境
APP_ENV=production ./myapp   # 生产环境
```

---

### 4.7 跨平台兼容性问题

#### Q19: Windows 和 Linux 配置文件路径不一致

**现象**：
在 Windows 上配置文件路径为 `C:\Users\username\.alibabacloud\credentials.ini`，但代码中硬编码了 Linux 路径。

**根本原因**：
跨平台路径分隔符不同（Windows 使用 `\`，Linux/macOS 使用 `/`）。

**解决方案**：

1. **使用环境变量**（推荐）：
   ```bash
   # Linux/macOS
   export ALIBABA_CLOUD_CREDENTIALS_FILE=/home/user/.alibabacloud/credentials.ini
   
   # Windows
   set ALIBABA_CLOUD_CREDENTIALS_FILE=C:\Users\user\.alibabacloud\credentials.ini
   ```

2. **在代码中使用平台无关的路径**：
   ```cpp
   #include <cstdlib>
   
   std::string getHomePath() {
   #ifdef _WIN32
       const char* home = std::getenv("USERPROFILE");
   #else
       const char* home = std::getenv("HOME");
   #endif
       return home ? home : "";
   }
   
   std::string credPath = getHomePath() + 
   #ifdef _WIN32
       "\\.alibabacloud\\credentials.ini";
   #else
       "/.alibabacloud/credentials.ini";
   #endif
   ```

3. **使用 C++17 std::filesystem**（如果编译器支持）：
   ```cpp
   #include <filesystem>
   
   namespace fs = std::filesystem;
   fs::path credPath = fs::path(getHomePath()) / ".alibabacloud" / "credentials.ini";
   ```

---

#### Q20: macOS 上提示 "dyld: Library not loaded"

**现象**：
```
dyld: Library not loaded: @rpath/libalibabacloud_credential.dylib
Reason: image not found
```

**根本原因**：
macOS 动态库加载路径（RPATH）配置不正确，或库未安装到标准位置。

**解决方案**：

1. **设置 `DYLD_LIBRARY_PATH`**（临时方案）：
   ```bash
   export DYLD_LIBRARY_PATH=/usr/local/lib:$DYLD_LIBRARY_PATH
   ./your_app
   ```

2. **重新安装库到系统路径**：
   ```bash
   cd cmake_build
   sudo cmake --install .
   ```

3. **检查库安装位置**：
   ```bash
   ls -l /usr/local/lib/libalibabacloud_credential.dylib
   ```

4. **修复 RPATH**（高级）：
   ```bash
   install_name_tool -add_rpath /usr/local/lib your_app
   ```

---

### 4.8 程序运行时问题

#### Q21: 程序运行时崩溃或段错误（Segmentation Fault）

**现象**：
```
Segmentation fault (core dumped)
```

**可能原因**：

1. **空指针访问**：
   ```cpp
   // 错误示例
   std::shared_ptr<Models::Config> config;  // 未初始化
   Client client(config);  // 传入空指针
   ```

2. **内存管理错误**：
   ```cpp
   // 错误示例
   Models::Config* config = new Models::Config();
   Client client(*config);
   delete config;  // 过早释放
   // client 仍在使用已释放的内存
   ```

3. **线程安全问题**：
   多线程并发访问同一个 Client 实例未加锁。

**解决方案**：

1. **确保对象生命周期**：
   ```cpp
   // 正确示例
   auto config = std::make_shared<Models::Config>();
   config->setType("access_key")
         .setAccessKeyId("...")
         .setAccessKeySecret("...");
   Client client(config);  // config 通过 shared_ptr 共享所有权
   ```

2. **使用智能指针**：
   ```cpp
   std::shared_ptr<Client> client = std::make_shared<Client>();
   ```

3. **线程安全保护**：
   ```cpp
   #include <mutex>
   
   std::mutex mtx;
   Client client;
   
   void getCredentialSafely() {
       std::lock_guard<std::mutex> lock(mtx);
       auto credential = client.getCredential();
   }
   ```

4. **启用调试符号定位问题**：
   ```bash
   cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug
   cmake --build build
   gdb ./your_app
   # (gdb) run
   # (gdb) bt  # 查看堆栈跟踪
   ```

---

#### Q22: 内存泄漏或资源未释放

**现象**：
程序长时间运行后内存占用持续增长。

**可能原因**：

1. **手动 `new` 后未 `delete`**：
   ```cpp
   // 错误示例
   for (int i = 0; i < 10000; ++i) {
       Models::Config* config = new Models::Config();
       Client client(*config);
       // 忘记 delete config
   }
   ```

2. **循环引用**（较少见）。

**解决方案**：

1. **使用 RAII 和智能指针**：
   ```cpp
   // 正确示例
   for (int i = 0; i < 10000; ++i) {
       auto config = std::make_shared<Models::Config>();
       Client client(config);
       // 自动释放，无需手动管理
   }
   ```

2. **使用内存检测工具**：
   ```bash
   # Valgrind (Linux)
   valgrind --leak-check=full ./your_app
   
   # AddressSanitizer (GCC/Clang)
   g++ -fsanitize=address -g your_code.cpp -o your_app
   ./your_app
   ```

---

#### Q23: 程序在容器环境（Docker）中无法获取 ECS 角色凭证

**现象**：
在 Docker 容器中运行的程序无法访问 ECS 元数据服务。

**根本原因**：
1. Docker 网络隔离，无法访问宿主机的 `100.100.100.200`
2. 容器未使用主机网络模式

**解决方案**：

1. **使用主机网络模式**（推荐）：
   ```bash
   docker run --network=host your_image
   ```

2. **将凭证通过环境变量传入容器**：
   ```bash
   docker run \
     -e ALIBABA_CLOUD_ACCESS_KEY_ID=xxx \
     -e ALIBABA_CLOUD_ACCESS_KEY_SECRET=yyy \
     your_image
   ```

3. **挂载配置文件到容器**：
   ```bash
   docker run \
     -v ~/.alibabacloud:/root/.alibabacloud:ro \
     your_image
   ```

---

### 4.9 依赖库问题

#### Q24: 运行时提示 "error while loading shared libraries: libssl.so.x"

**现象**：
```
error while loading shared libraries: libssl.so.1.1: cannot open shared object file
```

**根本原因**：
系统缺少 OpenSSL 运行时库，或库版本不匹配。

**解决方案**：

1. **安装 OpenSSL 运行时库**：
   ```bash
   # Ubuntu/Debian
   sudo apt-get install libssl1.1
   
   # CentOS/RHEL
   sudo yum install openssl-libs
   ```

2. **检查库文件是否存在**：
   ```bash
   ldconfig -p | grep libssl
   ```

3. **手动指定库路径**：
   ```bash
   export LD_LIBRARY_PATH=/usr/local/lib:$LD_LIBRARY_PATH
   ```

4. **使用 `ldd` 检查依赖**：
   ```bash
   ldd your_app
   # 查看是否有 "not found"
   ```

---

#### Q25: Windows 上提示 "找不到 libcurl.dll"

**现象**：
```
无法启动此程序，因为计算机中丢失 libcurl.dll
```

**根本原因**：
依赖的 DLL 文件未在程序同一目录或系统 PATH 中。

**解决方案**：

1. **将 DLL 复制到程序目录**：
   ```powershell
   # 从 vcpkg 安装目录复制
   copy C:\vcpkg\installed\x64-windows\bin\libcurl.dll .\
   copy C:\vcpkg\installed\x64-windows\bin\libcrypto-*.dll .\
   copy C:\vcpkg\installed\x64-windows\bin\libssl-*.dll .\
   ```

2. **使用 CMake 自动复制**（已在 CMakeLists.txt 中配置，第 176-198 行）：
   重新编译即可自动复制 DLL。

3. **添加到系统 PATH**：
   ```powershell
   $env:PATH += ";C:\vcpkg\installed\x64-windows\bin"
   ```

---

### 4.10 调试与日志

#### Q26: 如何开启详细日志以排查问题

**现象**：
凭证获取失败，但不知道具体在哪一步出错。

**解决方案**：

1. **捕获异常并输出详细信息**：
   ```cpp
   #include <darabonba/Exception.hpp>
   
   try {
       Client client;
       auto credential = client.getCredential();
   } catch (const Darabonba::Exception& e) {
       std::cerr << "Darabonba Exception: " << e.what() << std::endl;
       std::cerr << "Message: " << e.message() << std::endl;
       // 如果有更多字段，输出它们
   } catch (const std::exception& e) {
       std::cerr << "Standard Exception: " << e.what() << std::endl;
   }
   ```

2. **添加调试日志**（SDK 未内置日志系统）：
   ```cpp
   #include <iostream>
   
   #define DEBUG_LOG(msg) std::cout << "[DEBUG] " << msg << std::endl
   
   DEBUG_LOG("开始获取凭证...");
   Client client;
   DEBUG_LOG("Client 创建成功");
   
   auto credential = client.getCredential();
   DEBUG_LOG("凭证获取成功: " << credential.accessKeyId());
   ```

3. **检查环境变量**：
   ```cpp
   #include <cstdlib>
   
   void printEnv(const char* name) {
       const char* value = std::getenv(name);
       std::cout << name << " = " << (value ? value : "(未设置)") << std::endl;
   }
   
   printEnv("ALIBABA_CLOUD_ACCESS_KEY_ID");
   printEnv("ALIBABA_CLOUD_ACCESS_KEY_SECRET");
   printEnv("ALIBABA_CLOUD_ECS_METADATA");
   ```

4. **使用网络抓包工具**（排查网络问题）：
   ```bash
   # Wireshark 或 tcpdump
   sudo tcpdump -i any host sts.aliyuncs.com -w capture.pcap
   ```

---

### 4.11 性能优化

#### Q27: 频繁调用 `getCredential()` 影响性能

**现象**：
每次 API 调用都执行 `getCredential()`，导致性能开销较大。

**根本原因**：
`getCredential()` 可能涉及网络请求（如刷新 STS Token），频繁调用影响性能。

**解决方案**：

1. **复用 Client 实例**：
   ```cpp
   // 错误示例：每次创建新 Client
   for (int i = 0; i < 1000; ++i) {
       Client client;
       auto cred = client.getCredential();
       // 使用 cred
   }
   
   // 正确示例：复用 Client
   Client client;
   for (int i = 0; i < 1000; ++i) {
       auto cred = client.getCredential();  // 内部有缓存机制
       // 使用 cred
   }
   ```

2. **缓存凭证**（需注意刷新时机）：
   ```cpp
   Client client;
   auto credential = client.getCredential();
   
   // 短时间内复用
   for (int i = 0; i < 100; ++i) {
       std::string ak = credential.accessKeyId();
       // 使用 ak
   }
   
   // 长时间运行需定期刷新
   // 每 30 分钟刷新一次
   ```

3. **对于长期 AccessKey**：
   ```cpp
   // AccessKey 不会过期，可以缓存
   Models::Config config;
   config.setType("access_key")
         .setAccessKeyId("...")
         .setAccessKeySecret("...");
   Client client(config);
   
   // 获取一次后可长期使用
   auto credential = client.getCredential();
   ```

---

## 5. 最佳实践总结

1. **优先使用默认凭证提供者链**：无需硬编码凭证类型，自动适配多种环境
2. **生产环境使用 ECS RAM 角色或 OIDC**：避免长期 AccessKey 泄露风险
3. **配置文件权限设置为 600**：防止敏感信息泄露
4. **使用智能指针管理对象生命周期**：避免内存泄漏
5. **合理设置超时时间**：根据网络环境调整，避免过短或过长
6. **启用 IMDSv2**：提升 ECS 元数据服务安全性
7. **定期轮转 AccessKey**：降低密钥泄露风险
8. **区分开发/测试/生产环境**：使用不同的凭证和配置
9. **捕获并处理异常**：提供友好的错误提示
10. **定期更新 SDK**：获取最新功能和安全修复

---

## 6. 相关资源

- **GitHub 仓库**：[https://github.com/aliyun/credentials-cpp](https://github.com/aliyun/credentials-cpp)
- **阿里云文档中心**：[https://help.aliyun.com/](https://help.aliyun.com/)
- **AccessKey 管理**：[https://usercenter.console.aliyun.com/#/manage/ak](https://usercenter.console.aliyun.com/#/manage/ak)
- **RAM 控制台**：[https://ram.console.aliyun.com/](https://ram.console.aliyun.com/)
- **问题反馈**：[https://github.com/aliyun/credentials-cpp/issues](https://github.com/aliyun/credentials-cpp/issues)

---

**文档版本**：1.0  
**最后更新**：2024-12-08  
**适用 SDK 版本**：0.1.0+
